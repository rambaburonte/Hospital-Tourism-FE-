import React from 'react';

const ViewDashboard = () => {
    return (
        <div className="p-6">
            <h1 className="text-2xl font-bold">View Dashboard</h1>
            <p>This is the main dashboard view for sub-admins.</p>
            {/* Add dashboard components here */}
        </div>
    );
};

export default ViewDashboard;
